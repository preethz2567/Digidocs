server.port = 8081
spring.application.name=digidocs

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/digidocs}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Preethi@1224}

spring.jpa.hibernate.ddl-auto=${DB_DDL_AUTO:update}
spring.jpa.show-sql=true

spring.jpa.properties.hibernate.format_sql=true
jwt.secret=${JWT_SECRET:ReplaceThisWithAVeryLongSecretKeyAtLeast32CharactersLong123456}
jwt.expiration=${JWT_EXPIRATION:86400000}
file.upload-dir=uploads
storage.provider=local
aws.s3.bucket=${AWS_S3_BUCKET:}
aws.region=${AWS_REGION:}
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000
